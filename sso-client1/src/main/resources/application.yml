server:
  port: 8081
spring:
  application:
    name: sso-client1
  main:
    banner-mode: off
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost:8079/uaa"
logging:
  level:
    root: info
    "org.springframework.security.web": info
