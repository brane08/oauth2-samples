POSTGRES_PASSWORD=changeit
DEFAULT_CLIENT_SECRET=changeit

# Uncomment to have sso-gateway-mvc trust the Sm_user header from the httpd-sso-sim
# service instead of doing its own OIDC login (see docker/httpd/sso-sim.conf).
# GATEWAY_PROFILE=ping-preauth
